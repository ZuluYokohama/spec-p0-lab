# CERTIFY

Date: 2026-08-24

## Certified (this session)

- Repo `spec-p0-lab` exists and contains executable P0 probes + unit tests.
- Grok 4.6 prior `premature-sheafification` is ingested as a binding negative: sheaf layer lost to a 1-D pairwise statistic; P2 must beat cosine/NLI or die.
- Grok 4.6 prior-art PDFs ingested: SPEC length-gen claim is empty as *published empirical claim*; QK-norm arm is mandatory; NB-LoRA × length is open.
- No GPU experiment was executed in this session. Host sandbox was over capacity. Numbers in `results/` will exist only after `python -m src.spec_p0` is run on a machine with numpy.

## Not certified

- Length generalization.
- SPEC beats QK-norm.
- LoRA destroys length-gen on a real LM.
- Sheaf energy as an agent gate.
- Any frontier-competitive small model.

## Next certified object

A `results/p0_synthetic.json` produced by the checked-in script, with git commit recorded, plus pytest green. That is still only P0.
