# High-merit next actions

Order is load-bearing. Do not reorder for prestige.

## Now (first chain still running)

1. Confirm the live prereg hash contains the G4 empty-set branch
   (`empty_p99_policy: NA`, 0-index `[0, L)`). If that hash does not
   contain it, L=512 p99 on the first run is not interpretable: write `NA`,
   do not invent a number.
2. Leave training alone.

## When eval_norm.py returns

3. Admit the bundle only through `src/g4_bridge.py`.
4. Fill G1–G3 under the locked v1.1/v1.2 definitions.
5. Write G4 as two rows. L=512 p99 is `NA`. L=1024 p99 and entropy share
   positions by grid accident — still two rows.
6. Licensed noun remains σ-reparam until G3 says otherwise.

## Only if G1–G3 pass and G4 is not an M2-style uniform ceiling

7. P1 interaction: σ-cap × QK-norm × NB-LoRA on one small coder
   (1B–3B class), train length ≤ 2k, eval RULER/copy at 2×–4×.
   Kill if in-distribution bpb regresses > locked epsilon.
8. Do not introduce sheaf energy until a cheaper gate (conformal s·Q or
   raw entropy) loses a pre-registered comparison.

## Refused until those gates fire

- 27B pretrain
- "frontier-competitive small LLM" as a present-tense claim
- HF Jobs spend without a passing P1 protocol on disk
